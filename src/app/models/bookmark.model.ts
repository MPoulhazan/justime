export class Bookmark {

    lineId: string;
    stopId: string;

    constructor(
        lineId: string,
        stopId: string
    ) {
        this.lineId = lineId;
        this.stopId = stopId;
    }
}
